# GharSehat 🩹
- The app is deployed at https://gharsehat.onrender.com.
  ## DEMO VIDEO LINK.
- correct demo video https://youtu.be/ekacSjaTeYc


**A computer-vision-powered burn wound recovery companion for Indian home caregivers — it helps surface day-to-day visual changes in wound photos and prioritizes cases for doctor review.**

Built for techspire hackathon.

> ⚠️ **GharSehat detects measurable *visual change*, never a diagnosis.** Every output describes change only, such as *"redness area increased"* or *"visual change detected."* It never claims infection, necrosis, pus, slough, wound depth, or any medical condition. It is **not a medical diagnosis** and is no substitute for a doctor.

---

## The problem

After a burn patient is discharged, recovery happens at home. A family caregiver — often with no medical training — has to judge, day to day, whether the wound is healing or getting worse. Small changes are hard to see by eye, clinics may be far, and by the time something looks obviously wrong it may already be serious.

Informal photo-sharing over messaging apps is also hard for doctors to review systematically. Photos may be scattered, symptoms may be missing, and there is often no structured timeline of how the wound changed over multiple days.

---

## The idea

The caregiver photographs the wound each day. GharSehat compares today's photo with yesterday's, measures **visual change**, combines it with a short symptom checklist, and gives the caregiver a clear Hindi/English verdict and next step.

At the same time, the completed check-in is saved into a doctor portal, where doctors can review patients in risk order, inspect the day-wise timeline, and expand advanced non-diagnostic photo-analysis details when needed.

Two core safety principles drive the design:

1. **Detect change, never diagnose.**
2. **Prioritize sensitivity over precision** — in this prototype, a cautious false alarm is preferable to missing a possible deterioration.

---

## What it does

### Caregiver app

* **Guided photo capture** with a live camera quality check for lighting, blur, and framing.
* **Upload fallback** for controlled demo images or when camera capture is unreliable.
* **Symptom checklist** covering fever, smell, spreading redness, discharge, and increasing pain.
* **Instant plain-language result** with:

  * headline guidance,
  * reason summary,
  * next step,
  * care-level meter,
  * photo-comparison summary,
  * safety disclaimer.
* **Bilingual support** in Hindi and English.
* **Fixed medical message templates** — caregiver-facing advice is not generated by an LLM.
* **Escalation guidance** from stable/watch closely to show doctor today or call 108.

### Doctor portal

* **Risk-sorted patient list** — red first, then amber, then green.
* **Per-patient recovery timeline** with day-wise history.
* **Score trend** for quick longitudinal review.
* **Wound photo strip** showing hardcoded demo history and submitted check-in photos.
* **Submitted yesterday/today photos** from caregiver check-ins.
* **Latest symptoms and scores** for doctor review.
* **Advanced photo analysis** shown as a collapsed section for doctors only.
* **Live updates by polling** — submitted check-ins appear in the portal without automated push notifications.

---

## How the scoring works

A check-in produces a **change score** from the photo comparison and a **symptom score** from the checklist.

```text
symptom weights:
spreading_redness = 30
fever = 25
discharge = 20
smell = 15
increasing_pain = 10

symptom_score = sum of weights for symptoms marked true

final_score = change_score × 0.5 + symptom_score × 0.5
```

`final_score` maps to a status and caregiver action:

| final_score | status   | caregiver action       |
| ----------- | -------- | ---------------------- |
| `< 30`      | 🟢 green | Continue care          |
| `30–59`     | 🟡 amber | Watch closely          |
| `60–74`     | 🔴 red   | Show doctor today      |
| `≥ 75`      | 🔴 red   | Call 108 / urgent care |

The numeric scores remain available for doctor review, but the caregiver result screen emphasizes plain-language guidance rather than raw numbers.

---

## Photo comparison layer

GharSehat has two photo-analysis modes.

### 1. `/analyze` — demo/mock analysis

This endpoint returns a fixed, reliable visual-change score.

It is intentionally kept as a **demo safety net** so the app remains usable even if lighting, camera permissions, or image quality are poor during a live demo.

### 2. `/analyze-real` — real OpenCV analysis

This endpoint runs an experimental OpenCV-based photo comparison.

The pipeline:

1. Decodes yesterday and today images.
2. Resizes the photos for consistent processing.
3. Crops the central region of interest.
4. Runs multi-threshold HSV redness analysis.
5. Keeps only thresholds showing positive redness growth.
6. Uses the median of valid redness-growth thresholds.
7. Computes supporting non-diagnostic visual metrics:

   * dark visual area change,
   * yellow/cream visual area change,
   * approximate visual-region area change,
   * combined border change.
8. Returns visual-change values and debug information.

The real CV pipeline is designed for **serial comparison under guided capture**, not one-off diagnosis from arbitrary wound photos.

Important limitations:

* no scale normalization,
* no lighting normalization,
* no true wound segmentation,
* no depth assessment,
* no diagnosis of infection, necrosis, pus, slough, or tissue type.

The advanced visual metrics are shown only in the doctor portal and are collapsed by default to avoid overwhelming caregivers.

---

## Patient-friendly result guidance

The `/assess` endpoint keeps doctor-useful numeric fields, but also returns caregiver-friendly interpretation fields.

Examples include:

```text
patient_title
patient_summary
reason_summary
next_step
care_level_label
care_level_position
visual_change_label
```

Example caregiver result:

```text
Doctor review recommended

This check-in shows enough change that a doctor should review it.

Why this result:
- The photo comparison showed a clear visual change.
- Redness was reported as spreading.

Next step:
Show this check-in to your doctor today.
```

The result screen is designed to answer:

```text
What happened?
Why did the app say this?
What should I do next?
```

---

## Doctor-side advanced photo analysis

When a real OpenCV check-in is saved, the doctor portal can show a collapsed **Advanced photo analysis** section.

Example labels:

```text
Red visual area: High
Dark visual area: Mild
Yellow/cream visual area: Low
Approx. visual-region change: Mild
Combined border change: Low

Non-diagnostic visual features for doctor review.
```

The portal intentionally shows severity labels rather than raw pixel/debug counts. These metrics are supporting signals for review, not diagnostic outputs.

---

## Tech stack

| Layer           | Tools                                                         |
| --------------- | ------------------------------------------------------------- |
| Backend         | Python 3.11+, Flask, flask-cors                               |
| Computer vision | OpenCV, NumPy                                                 |
| Frontend        | React 18, TypeScript, Vite                                    |
| UI              | Tailwind CSS, lucide-react                                    |
| Routing         | React Router                                                  |
| Storage         | In-memory demo data + local JSON file for submitted check-ins |

This is a hackathon prototype:

```text
No authentication
No production database
No cloud deployment requirement
No ML model
No LLM calls
No clinical validation
```

Patient data is hardcoded demo data. Submitted check-ins, uploaded photos, and doctor reviews persist to local JSON/file storage only. On Render this storage is **ephemeral** — see [Persistence on Render](#persistence-on-render).

---

## Project structure

```text
gharsehat-backend/
├── app.py              # Flask app, route wiring, API endpoints, serves built frontend
├── assess.py           # Symptom scoring + patient-friendly guidance
├── messages.py         # Fixed Hindi/English caregiver message templates
├── analyze.py          # OpenCV visual-change comparison for /analyze-real
├── capture_check.py    # Camera-frame quality checks: lighting, blur, framing
├── data.py             # Hardcoded demo patients + 5-day histories
├── storage.py          # Submitted check-in persistence + photo uploads
├── tune_mask.py        # Local OpenCV threshold-tuning helper
├── requirements.txt
└── frontend/
    ├── package.json
    └── src/
        ├── pages/      # RoleSelect, Home, CheckIn, Symptoms, Result, DoctorPortal
        ├── components/ # Shared UI components
        ├── context/    # Check-in flow + language state
        └── lib/        # API clients, status/timeline helpers
```

---

## Running it locally

### 1. Backend

```bash
cd gharsehat-backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
```

The backend runs at:

```text
http://localhost:5000
```

> **macOS note:** port 5000 may be used by AirPlay Receiver. If Flask cannot bind to port 5000, disable it under **System Settings → General → AirDrop & Handoff → AirPlay Receiver**, or change the Flask port.

---

### 2. Frontend

For development:

```bash
cd frontend
npm install
npm run dev
```

For production build:

```bash
cd frontend
npm install
npm run build
```

Once the frontend is built, Flask serves the app directly from `frontend/dist`.

Open:

```text
http://localhost:5000
```

---

## API reference

| Method | Endpoint                | Purpose                                                             |
| ------ | ----------------------- | ------------------------------------------------------------------- |
| `GET`  | `/health`               | Backend liveness check                                              |
| `POST` | `/assess`               | Combines visual change + symptoms into status, action, and guidance |
| `POST` | `/analyze`              | Mock photo comparison; fixed reliable demo score                    |
| `POST` | `/analyze-real`         | Real OpenCV photo comparison + advanced visual metrics              |
| `POST` | `/capture-check`        | Live camera-frame quality check                                     |
| `POST` | `/checkins`             | Saves a completed check-in with photos/scores                       |
| `GET`  | `/patients`             | Doctor portal patient list                                          |
| `GET`  | `/patient/<id>/history` | Full patient timeline and submitted check-ins                       |
| `GET`  | `/uploads/<path>`       | Serves locally saved check-in photos                                |

---

## Example: `/assess`

```bash
curl -s -X POST http://localhost:5000/assess \
  -H "Content-Type: application/json" \
  -d '{"change_score": 68, "symptoms": {"fever": true, "spreading_redness": true}}'
```

Example response:

```json
{
  "status": "red",
  "final_score": 61.5,
  "change_score": 68,
  "symptom_score": 55,
  "action": "show_doctor_today",
  "patient_title": "Doctor review recommended",
  "next_step": "Show this check-in to your doctor today.",
  "message_hindi": "कल से महत्वपूर्ण बदलाव दिखा है। कृपया आज ही डॉक्टर को दिखाएँ।",
  "message_english": "Significant change detected since yesterday. Please show this to your doctor today.",
  "disclaimer_english": "Not a medical diagnosis. Always consult your doctor."
}
```

---

## Example: `/analyze-real`

```bash
curl -s -X POST http://localhost:5000/analyze-real \
  -F "yesterday=@test_images/baseline.jpg" \
  -F "today=@test_images/today.jpg" | python -m json.tool
```

Example response fields:

```json
{
  "change_score": 96.4,
  "redness_delta": 100.0,
  "border_change": 0.0,
  "dark_area_delta": 13.55,
  "yellow_area_delta": 0.0,
  "wound_area_delta": 13.8,
  "combined_border_change": 0.0,
  "mock": false,
  "disclaimer": "This checks visual change between photos only. It is not a medical diagnosis."
}
```

Actual values depend on the uploaded photos.

---

## Example: `/checkins`

`POST /checkins` saves a completed check-in.

Required form fields:

```text
patient_id
today
change_score
final_score
status
symptoms
```

Optional form fields:

```text
yesterday
action
redness_delta
border_change
dark_area_delta
yellow_area_delta
wound_area_delta
combined_border_change
```

Submitted photos are saved locally under `uploads/`, and metadata is stored in `checkins.json`.

---

## Demo flow

Recommended live demo path:

```text
1. Open the app.
2. Choose Caregiver.
3. Start Ravi’s check-in.
4. Select Real OpenCV analysis.
5. Upload prepared yesterday/today wound photos.
6. OpenCV returns a high visual change score.
7. Select “spreading redness” as the symptom.
8. Result screen shows “Doctor review recommended.”
9. Check-in is saved for doctor review.
10. Open the doctor portal.
11. Ravi appears as a high-priority patient.
12. Open Ravi’s history.
13. Submitted photos and scores appear in the timeline.
14. Expand Advanced photo analysis if judges ask about the technical layer.
```

Recommended demo sentence:

```text
GharSehat does not diagnose. It compares daily wound photos and caregiver-reported symptoms to prioritize patients for doctor review.
```

---

## Demo data

The prototype currently uses hardcoded demo patients and histories.

The main demo patient is:

```text
Ravi
Left forearm
2nd-degree burn
Day 5
```

Demo patient data is intentionally hardcoded for the hackathon prototype. Submitted check-ins are stored locally and appended to the demo timeline.

Runtime data is not committed.

---

## Responsible-AI and clinical safety guardrails

GharSehat is not a medical device and is not clinically validated.

It does not diagnose:

```text
infection
necrosis
pus
slough
wound depth
burn severity
clinical deterioration
```

It uses safe wording:

```text
visual change
photo comparison
symptoms reported
doctor review
urgent care recommended
non-diagnostic visual features
```

Guardrails implemented:

* caregiver-facing advice uses fixed templates,
* no LLM-generated medical advice,
* Hindi messages use Devanagari,
* every result includes a medical disclaimer,
* advanced photo metrics are doctor-side and collapsed by default,
* mock analysis remains available as a demo safety fallback,
* doctor portal receives submitted check-ins but no automated doctor notification is claimed.

---

## Limitations

Current limitations include:

* not clinically validated,
* not production-ready,
* no authentication,
* no database-backed persistence,
* no secure cloud image storage,
* local JSON/file storage only,
* no scale/reference-card normalization,
* no lighting normalization,
* no true wound boundary segmentation,
* no wound area calibration,
* no confirmed epithelialization/healing percentage,
* no clinical diagnosis of tissue type or infection,
* OpenCV outputs may be affected by shadows, dressings, skin tone, background, angle, and camera distance,
* advanced visual features are supporting signals only,
* real-world performance requires validation on de-identified clinical image sequences.

---

## Scientific and clinical motivation

Digital wound analysis and planimetry approaches can estimate healing-related parameters using pixel areas when wound boundaries and open wound regions are identified. However, reliable assessment often requires clinician judgment or manual marking of wound regions, especially because wound tissues vary in appearance and are difficult to distinguish using color alone.

GharSehat currently implements an early automated visual-change layer and treats all outputs as non-diagnostic. Future versions could add clinician-guided wound boundary confirmation and validated wound-area tracking.

---

## Future work

### Clinical workflow

* clinician review queue,
* doctor comments and decision logging,
* escalation history,
* patient-specific follow-up schedule,
* physiotherapy and aftercare education videos,
* dressing care guidance,
* multilingual caregiver education,
* follow-up adherence tracking.

### Computer vision

* validated wound boundary segmentation,
* reference-card or scale normalization,
* lighting normalization,
* day-wise wound area tracking,
* clinician-guided planimetry,
* epithelialization/healing percentage estimation after validation,
* larger de-identified burn/wound image dataset testing,
* clinical validation against doctor-reviewed labels.

### Product and deployment

* authentication,
* role-based access,
* secure image storage,
* database-backed persistence,
* deployment-ready backend,
* audit logs,
* hospital dashboard integration,
* notification system.

---

## Development notes

### Runtime files not to commit

The following are local/demo runtime artifacts and should not be committed:

```text
uploads/
checkins.json
checkins.json.tmp
doctor_reviews.json
doctor_reviews.json.tmp
test_images/
```

### Frontend checks

Before pushing major frontend changes:

```bash
npm run --prefix frontend typecheck
npm run --prefix frontend build
```

### Backend quick checks

```bash
curl -s http://localhost:5000/health | python -m json.tool
```

### Backend tests

Happy/sad-path smoke tests for `/assess`, `/patient/<id>/review`, and `/checkins`:

```bash
pip install -r requirements-dev.txt
python -m pytest
```

Tests redirect persistence to a temp dir, so they never touch real runtime data.

### Python version

The backend uses `X | None` type syntax and requires **Python 3.11+** (3.9 fails
to import `analyze.py`). Pinned via `runtime.txt` and `.python-version`.

### Persistence on Render

Check-ins, uploaded photos, and doctor reviews are stored as local files
(`checkins.json`, `uploads/`, `doctor_reviews.json`). **Render's filesystem is
ephemeral**: these reset on every deploy and container restart. This is accepted
for the demo (no database, per scope). Practical guidance:

* Do not redeploy right before a live demo — it wipes submitted data.
* Data created during a running session lasts until the next restart.
* For durable storage, attach a Render persistent disk or add a database
  (out of hackathon scope).

```bash
curl -s -X POST http://localhost:5000/analyze-real \
  -F "yesterday=@test_images/baseline.jpg" \
  -F "today=@test_images/today.jpg" | python -m json.tool
```

---

## Hackathon positioning

GharSehat should be presented as:

```text
A functional prototype for structured post-discharge burn wound monitoring and doctor review.
```

It should not be presented as:

```text
A clinically validated diagnostic AI system.
```

The strongest product story is:

```text
Caregivers already take wound photos, but those photos are unstructured and difficult for doctors to track. GharSehat turns daily photo check-ins and symptom reporting into a structured timeline and prioritization dashboard for doctor review.
```

---

## Current status

```text
Functional local prototype
Real OpenCV analysis available
Mock fallback available
Check-in saving implemented
Doctor portal implemented
Advanced non-diagnostic visual metrics available
Patient-friendly result guidance implemented
Not clinically validated
Not production-ready
```

---

## Final note

Built with care for caregivers.

**Not a medical device. Not a diagnosis. Always consult a doctor.**
